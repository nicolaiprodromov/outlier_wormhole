# Outlier Wormhole Configuration
# Copy this file to .env and fill in your credentials

# Outlier.ai Credentials (REQUIRED)
OUTLIER_EMAIL=your-email@example.com
OUTLIER_PASSWORD=your-password

# Service Ports (Optional - defaults shown)
wormhole_port=8765
chrome_debug_port=9222
oai_port=11434

# Service Hostnames (Optional - for Docker Compose these are set automatically)
# wormhole_server_host=wormhole-server
# wormhole_host=0.0.0.0
# oai_host=0.0.0.0

# Chrome Configuration (Optional)
# chrome_user_data_dir=/tmp/chrome-debug
